import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-plugin-app-tracking-transparency-demo-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
